import { StyleSheet } from 'react-native';
import { Colors } from '../../../utils/Colors';

export const PostStyles = StyleSheet.create({
  container: { padding: 8, textDecorationColor: Colors.Text },
  title: {
    fontSize: 18,
    fontWeight: '700',
    color: Colors.Text,
  },
});
