import { StyleSheet } from 'react-native';

export const PostCardStyles = StyleSheet.create({
  icon: {
    resizeMode: 'contain',
    marginTop: 4,
    marginLeft: 4,
    marginBottom: 'auto',
    width: 24,
    height: 24,
  },
});
